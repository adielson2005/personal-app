{
  "name": "personal-trainer-app",
  "version": "1.0.0",
  "description": "Personal Trainer App Backend",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
